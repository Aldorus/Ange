<h1>UtilisateurCours creation</h1>

<form action="{{ path('utilisateurcours_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('utilisateurcours') }}">
            Back to the list
        </a>
    </li>
</ul>
